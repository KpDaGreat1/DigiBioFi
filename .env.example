# ─────────────────────────────────────────────────────────
# DigiBioFi — Environment Variables
# Copy to .env and fill in all values before running.
# ─────────────────────────────────────────────────────────

# App
APP_NAME="DigiBioFi"
APP_ENV=development          # development | production
DEBUG=true
SECRET_KEY=change-me-to-a-long-random-secret-key-at-least-32-chars
BASE_URL=http://localhost:8000

# Database
# Local dev (SQLite — auto-created, no setup needed):
DATABASE_URL=sqlite:///./digibiofi.db
# Production (PostgreSQL):
# DATABASE_URL=postgresql://user:password@host:5432/digibiofi

# JWT
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=30

# File uploads
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE_MB=5

# Admin bootstrap (used by scripts/create_admin.py)
# REQUIRED: Must be set via environment variable — never commit real credentials
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-to-a-secure-password-at-least-8-chars

# Email (scaffold — not required for MVP local dev)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=noreply@example.com
EMAILS_FROM_NAME="DigiBioFi"

# CORS (comma-separated origins for API consumers)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
